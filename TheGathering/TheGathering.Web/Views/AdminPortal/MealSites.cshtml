@model IEnumerable<TheGathering.Web.Models.MealSite>

@{
    ViewBag.Title = "MealSites";
}

<div class="container-fluid">
    <h2>Manage Meal Sites</h2>

    <p>
        @Html.ActionLink("New Meal Site", "Create", "MealSite", null, new { @class = "btn btn-default" })
        @Html.ActionLink("Back To Portal Home", "Index", null, new { @class = "btn btn-default" })
    </p>

    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-default">
                <table class="table table-center">
                    <tr>
                        <th>
                            Address
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.City)
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.AddressLine1)
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.City)
                            </td>

                            <td>
                                @Html.ActionLink("Edit", "Edit", "MealSite", new { id = item.Id }, null) |
                                @Html.ActionLink("Details", "MealSiteDetails", new { id = item.Id }, null) |
                                @Html.ActionLink("Delete", "Delete", "MealSite", new { id = item.Id }, null)
                            </td>
                        </tr>
                    }

                </table>
            </div>
        </div>
    </div>
</div>

<script>
	@foreach (var item in Model)
	{
		@Html.Raw("locationAddresses.push('" + item.AddressLine1 + ", " + item.City + ", " + item.State + "');locationIds.push(" + item.Id + ");")
	}
</script>